(ns clj-eyes.controls
  (:require [ajax.core :refer [GET POST]]
  (:use jayq.core ))

(defn handle-select-transform []
  (POST "/select-transformation"
       {:params
        {:transform (val ($ :#select-transform))}}))

(-> ($ :#select-transform)
    (change handle-select-transform))
